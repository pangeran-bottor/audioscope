# Copy to .env and fill in.
# Only required for the LLM insight layer; the tool works without it (rule-based fallback).
OPENAI_API_KEY=sk-...

# Optional overrides:
# AUDIOSCOPE_LLM_PROVIDER=openai
# AUDIOSCOPE_LLM_MODEL=gpt-4o-mini
